Assert! when {{} {
    fn wow {} {
        puts step2
        error bad
    }
    fn hello {} {
        puts step1
        wow
    }

    When we are \
        \
        awake {
            try {
                hello
            } on error e {
                set st [info stacktrace]
                assert {[lindex $st 2] == 4}
                assert {[lindex $st 5] == 8}
                __exit 0
            }
    }
    Claim we are awake


}} with environment {}
