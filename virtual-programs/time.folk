Commit the clock time {
  Claim the clock time is [/ [clock milliseconds] 1000.0]
}

Every time the clock time is /time/ {
  After 50 milliseconds {
    Commit the clock time {
        Claim the clock time is [expr {$time + 0.05}]
    }
  }
}
